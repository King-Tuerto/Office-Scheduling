name: Keep Supabase Project Active

on:
  schedule:
    - cron: '0 8 * * 1,4'  # Every Monday and Thursday at 8:00 AM UTC
  workflow_dispatch:

jobs:
  ping-supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Send ping to Supabase
        env:
          SUPABASE_URL: https://vlbgfdabknaykwzkjdom.supabase.co
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          echo "Attempting to ping Supabase REST API..."
          curl -v "${SUPABASE_URL}/rest/v1/available_slots?select=*&limit=1" \
            -H "apikey: ${SUPABASE_SERVICE_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_SERVICE_KEY}" || echo "Curl command failed"
          
          echo "Full curl output above (look for HTTP code or error details)"
